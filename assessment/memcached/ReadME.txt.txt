To test the memcached run the script "test_memcached.py"

The defaul algorithm is set to "KETAMA".
  a) If you want to test "Modulo" algorithm pass "-m" with the command like "python test_memcached.py -m"
  b) Also you can pass "-k" to test "KETAMA" algorithm.
